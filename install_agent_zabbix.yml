---
- name: Install Zabbix Agent
  hosts: all
  become: yes
  vars:
    zabbix_server: ZabbixAgent-12f0d8a977b95a55.elb.us-east-1.amazonaws.com

  tasks:
    - name: Installer le paquet zabbix-agent
      apt:
        name: zabbix-agent
        state: present
      become: true

    - name: Modifier la configuration de l'agent Zabbix
      lineinfile:
        path: /etc/zabbix/zabbix_agentd.conf
        regexp: "{{ item.regexp }}"
        line: "{{ item.line }}"
        state: present
      with_items:
        - { regexp: '^Hostname=.*', line: 'Hostname={{ inventory_hostname }}' }
        - { regexp: '^Server=.*', line: 'Server={{ zabbix_server }}' }
        - { regexp: '^ServerActive=.*', line: 'ServerActive={{ zabbix_server }}' }

    - name: Démarrer et activer le service zabbix-agent
      service:
        name: zabbix-agent
        state: started
        enabled: true
      become: true

    - name: Restart zabbix-agent
      service:
        name: zabbix-agent
        state: restarted
      become: true